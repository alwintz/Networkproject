An app that uses TCP socket programming and client to server architecture, to allow users to signUp and login, send messages to roooms and to other users, change usernames,
send and respond to room invitations

Certain messages will be seen as commands if sent by the user. The commands are commented in the server.py and shouldn't be used by the client.
